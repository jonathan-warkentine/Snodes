  <header class="container-fluid rounded-top d-flex justify-content-between p-5 bg-primary">
      <h1>
        <a href="/" class="text-light">{ Snodes }</a>
      </h1>
      <nav>
        <!-- Conditionally render login or logout links -->
        {{#if logged_in}}
        <a href="/profile/{{user_id}}" class="text-light">my profile</a> |
            <button class="no-button" id="logout">logout</button>
        {{else}}
        <a href="/login">login</a>
        {{/if}}
      </nav>
  </header>

<header class="container-fluid rounded-bottom bg-secondary sticky-top">
  <div class="row p-3 d-flex justify-content-between ">
      <form id="search-form" class="col-auto d-flex flex-nowrap">
        <input id="searchbar" type="search" placeholder="Search Snodes..." class="rounded">
        <input type="submit" value="Search" class="ml-1 rounded">
      </form>
      <div class="col-1"></div>
      <nav class="col-auto d-flex flex-nowrap align-items-center">  
        <a class="text-light pr-2 d-flex align-items-center" href="/topsnodes">Top Snodes</a> |
        <a class="text-light pr-2 pl-2 align-items-center" href="/recents">Fresh Snodes</a> |
        <a class="text-light pr-2 pl-2 align-items-center" href="/tags">Top Cateogories</a> |
        <a class="text-light pr-2 pl-2 align-items-center" href="/draftsnode">Post Your Snode!</a>
      </nav>
  </div>
</header>

<script src="/js/searchbar.js"></script>